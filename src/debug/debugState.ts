/** Shared flags between the developer menu and the UI (kept tiny on purpose). */
export const debugState = {
  /** a debug tool (e.g. the measuring tape) wants plain mouse clicks on the canvas: no pointer lock */
  capturePointer: false,
  /** debug menu visible */
  visible: false,
};
